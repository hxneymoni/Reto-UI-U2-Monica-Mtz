import 'package:flutter/material.dart';

//COLOR
const backgroundcolor = Color(
    0xffFBFBFB); //define el color establecido como constante para el fondo

final theme = ThemeData(
    fontFamily:
        'Monserrat'); //indica que se utilizará la fuente de texto 'Monserrat' como la fuente predeterminada en toda la aplicación
